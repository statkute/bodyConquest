package main.com.bodyconquest.constants;

/** The enum Resource type. */
// Resources
public enum Resource {
  /** Protein resource type. */
  PROTEIN,
  /** Lipid resource type. */
  LIPID,
  /** Carbs resource type. */
  SUGAR
}